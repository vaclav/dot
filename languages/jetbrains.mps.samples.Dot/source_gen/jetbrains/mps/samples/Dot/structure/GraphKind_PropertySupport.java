package jetbrains.mps.samples.Dot.structure;

/*Generated by MPS */

import jetbrains.mps.smodel.PropertySupport;
import java.util.Iterator;
import jetbrains.mps.internal.collections.runtime.ListSequence;

public class GraphKind_PropertySupport extends PropertySupport {
  public boolean canSetValue(String value) {
    if (value == null) {
      return true;
    }
    Iterator<GraphKind> constants = ListSequence.fromList(GraphKind.getConstants()).iterator();
    while (constants.hasNext()) {
      GraphKind constant = constants.next();
      if (value.equals(constant.getName())) {
        return true;
      }
    }
    return false;
  }
  public String toInternalValue(String value) {
    if (value == null) {
      return null;
    }
    Iterator<GraphKind> constants = ListSequence.fromList(GraphKind.getConstants()).iterator();
    while (constants.hasNext()) {
      GraphKind constant = constants.next();
      if (value.equals(constant.getName())) {
        return constant.getValueAsString();
      }
    }
    return null;
  }
  public String fromInternalValue(String value) {
    GraphKind constant = GraphKind.parseValue(value);
    if (constant != null) {
      return constant.getName();
    }
    return "";
  }
}
